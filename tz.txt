

🚀 Общее описание проекта
VPN_GPT — интеллектуальная система продажи и сопровождения VPN-доступа, управляемая кастомным GPT-ассистентом.
Пользователь видит “ИИ‑помощника по выбору VPN”, но под капотом работает цепочка:
GPT → API → Telegram‑бот → Xray‑сервер → Morune‑платёжка → SQLite‑база.

GPT выступает центральным мозгом проекта и административной панелью, управляющей остальными компонентами через API.

⚙️ Архитектура системы

1. GPT‑ассистент
- Кастомный GPT, созданный через OpenAI Assistants API.
- Основной интерфейс общения с пользователем.
- Может вызывать actions — API‑эндпоинты (issue_key, renew_key, notify и т.д.).
- Отвечает за выдачу ключей, продление, уведомления, админ‑функции.

2. API‑сервер (FastAPI)
- Запущен на порту 8080.
- Реализует openapi_gpt.json.
- Отвечает за выполнение действий по запросу GPT или бота.
- Эндпоинты: /vpn/*, /users, /morune/*, /notify/*, /admin/*, /healthz.
- Авторизация админ‑вызовов через middleware (ADMIN_TOKEN).

3. Telegram‑бот (vpn‑bot.service)
- Расположен /root/VPN_GPT/bot.py.
- Запускается как systemd‑служба.
- На aiogram.
- Является транспортом между пользователем и GPT.
- Принимает команды (/start, /buy, /renew, /admin), пересылает GPT, отправляет ответы и VLESS‑ссылки.

4. Morune‑платёжка
- Интеграция через MORUNE_API_KEY, MORUNE_WEBHOOK_SECRET.
- При оплате вызывает /morune/paid.
- API создаёт VPN‑ключ, добавляет в Xray, записывает в БД и уведомляет клиента.

5. База данных (SQLite)
- Файл /root/VPN_GPT/dialogs.db, режим WAL.
- Таблицы:
  history — диалоги,
  vpn_keys — ключи (id, username, uuid, link, issued, expires, active).

6. VPN‑сервер (Xray)
- Протокол VLESS (без Reality), порт 2053.
- Конфиг /usr/local/etc/xray/config.json.
- Клиенты добавляются/удаляются API.
- Ссылка формируется как vless://UUID@HOST:PORT?encryption=none#VPN_GPT.
- HOST и PORT в .env.

7. Системные службы
- vpn‑bot.service — бот (активен).
- morune‑webhook.service — устарел.
- xray.service — VPN‑демон.
- api‑server — FastAPI (nohup, порт 8080).



🔁 Взаимодействие компонентов
1. Пользователь пишет боту → бот пересылает GPT.
2. GPT вызывает API (issue_key, renew_key, notify).
3. API выполняет: изменяет БД, Xray, уведомляет бота.
4. Бот отправляет пользователю результат (VLESS‑ссылка, QR).
5. При оплате Morune вызывает /morune/paid → API → создание ключа.

👨‍💼 Админ‑функции
- Вход по ADMIN_ID.
- Команды: /admin, /users, /expired, /broadcast, /backup.
- GPT также управляет этими действиями через API.

🌐 Веб‑сайт
- vpn-gpt.store — статичный HTML+CSS.
- УТП: “ИИ‑помощник подбирает VPN под твои цели”.
- Контакт: t.me/dobriyvpn_bot.
- Внизу — документы (Privacy Policy, Terms и т.д.).

🔄 CI/CD и деплой
- Репозиторий github.com/leaddesigner63/VPN_GPT.
- git credential.helper store — сохранён токен.
- GitHub Actions деплоит через SSH на 45.92.174.166 → /root/VPN_GPT.
- Автоматически перезапускает vpn‑bot.service.
- Секрет SSH_PRIVATE_KEY исправлен, деплой проверен.



VPN_GPT — законченная, автономная система, где GPT управляет всей экосистемой VPN‑продаж и поддержки через API‑интерфейс.
